import { Effect } from "../Effect.js";

export class IdleEffect extends Effect {

    constructor() {
        super();

        
    }

    update(camera, scene, mouse) {
        super.update(camera, scene, mouse);
    }
}